# Copy this file to API_keys.env (same folder) and fill in the keys for the
# providers you enable in ranking_pipeline/config.py. API_keys.env is
# git-ignored; only this example template is committed.
#
# The local vLLM provider (qwen-local) needs no key.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
